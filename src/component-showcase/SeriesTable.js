export default function Showcase({data = [], colnum = 5}) {

    function sliceBy(data, colnum) {
        let resultArray = []
        let nowArray = []
        
    }

    return <>
        {sliceBy(data, colnum)}
    </>
}